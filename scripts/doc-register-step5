#!/bin/sh
# Always use the checkout, even if an older distribution exists in site-packages.
set -eu
project_dir=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
export PYTHONPATH="$project_dir/src${PYTHONPATH:+:$PYTHONPATH}"
exec "$project_dir/.venv/bin/python" -m doc_register.step50 "$@"
